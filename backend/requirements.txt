pydantic>=2.0.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
numpy>=1.24.0
# CryptoMiniSat baseline (backend/cms_wrapper.py, metasolver benchmark) -- a
# competitor to beat, not part of the certified solve path.
pycryptosat>=5.11.0
