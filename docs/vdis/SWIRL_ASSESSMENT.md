# "Swirling a hyperbolic current in the moving infrastructure" — assessment

Operator: "consider swirling a hyperbolic current in our already moving
infrastructure." Assessment, not a build — and this one gets a
recommendation AGAINST, from our own freshly-measured evidence.

## What it decodes to

- "swirling current" = a rotational / vortical flow (curl, vorticity —
  a persistent angular drive).
- "hyperbolic" = the Poincaré-ball gyrovector geometry (gyro_ops, rotors).
- "already moving infrastructure" = the v4 Cartan moving frame that
  follows the conflict flow.

So: add a continuous rotational component to the moving frame — the frame
not merely translating with the conflict flow but *rotating* through it.

## Why the contract is not new

This is the rotor / torsion channel with a non-decaying rotational drive.
That family has been tested exhaustively and never helped:
- v1: torsion self-extinguished (state-proportional → absorbing).
- v2: prime-anchored torsion revived the channel; measured net-negative.
- v3: pair-torsion; smoothing, no primary-bar pass.
- v4: moving frame (the "already moving" part); net-neutral on decisions.
A continuous swirl reduces to per-variable phase precession, which is
exactly v4's MF3 (prime phases precessing in the frame) — tested,
marginal. No genuinely new input→invariant→action→benchmark contract.

## The decisive evidence against (from THIS session's validation)

The v8 validation (SWIRL's sibling turn) just established, adversarially:
the one large, replicated win in eight generations — a structural prior
halving decisions vs EVSIDS/LRB — is **plain variable degree** (eigenvector
centrality is 0.95–0.98 correlated with it and slightly worse). Every win
in the whole track cashed out to a CLASSICAL, NON-geometric mechanism:
- "Lagrange" → dual-descent multiplier → decision diversification.
- "self-reference" → eigenvector centrality → degree branching.
- "primes" → static asymmetric order → symmetry-breaking on pigeonhole.

The gyrovector geometry itself (hyperbolic ball, Möbius, rotors, moving
frame, torsion) has **never once moved the decision scalar favorably** in
eight generations. The pattern is now measured, not suspected: the
operator's conceptual intuitions are productive precisely when they cash
out to a classical algorithmic idea, and inert when they stay inside the
geometry. A swirling hyperbolic current stays inside the geometry.

## Recommendation

**Do not build it** — or if closure on the geometry is wanted, gate it
behind a one-probe liveness test with an explicitly strong-negative prior
(does the swirl enter the decision scalar non-trivially — which pure
geometry historically does not) and stop at the probe if it doesn't.
The "don't get carried away by enthusiasm" discipline the operator
invoked in the same message applies here most of all: the enthusiasm to
guard against is not only over-crediting the v8 win (handled — it's
degree branching), but also spending another generation on geometry the
evidence has already retired.

The high-value path is the opposite direction: bank the degree-branching
finding and run its replication (sizes × ≥50 seeds, wall-clock, vs
EVSIDS/LRB/CaDiCaL), dropping the entire gyrovector stack — which v1–v8
established was never the source of any win.
