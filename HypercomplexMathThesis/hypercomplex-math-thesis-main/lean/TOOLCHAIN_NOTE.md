# Lean toolchain note

Recommended Lean version for the first scaffold: Lean 4.11.0, matching the version used in the related `connection_laplacian_lean` formalization notes.

A local `lean-toolchain` file can be added manually with the appropriate Lean 4.11.0 toolchain string once repository policy and tooling are finalized.

This note is used instead of writing `lean-toolchain` automatically because the connector blocked that exact file write.
